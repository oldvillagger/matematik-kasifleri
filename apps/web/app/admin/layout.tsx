import "./admin.css";

export const metadata = {
  title: "Matematik Kâşifleri — İçerik Masası",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
